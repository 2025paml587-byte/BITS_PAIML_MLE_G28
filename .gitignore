# Generated data and model artifacts are tracked through DVC or regenerated locally.
data/data_folder/train/processed/*.csv

# Python
__pycache__/
*.pyc
.pytest_cache/

# MLflow local tracking store (experiment history is local, not versioned).
mlruns/
mlartifacts/

.venv

# Editor
.vscode/

# Docker Image
trip-duration-api.tar

# Monitoring log (runtime state, not versioned) - anchored to repo
# root so it doesn't also match src/monitoring/ (the source package).
/monitoring/

# Documents
SPEAKING_NOTES.md
